using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class _03MultiplicativeOperators : MonoBehaviour
{

	// Use this for initialization
	void Start ()
    {
        //Multiplicative Operators

            // * (Multiplication)
        Debug.Log("The product of 5 * 5 = " + (5 * 5));

            // / (Division)
        Debug.Log("The division of 9 / 3 = " + (9 / 3));

            // % (Remainder)
        Debug.Log("The remainder of 12 % 5 = " + (12 % 5));

    }

    // Update is called once per frame
    void Update () {
		
	}
}
